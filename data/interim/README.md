# data/interim

Intermediate products. Everything here is regenerable from `data/raw` by re-running the scripts, so nothing here is precious and nothing here is committed.

Delete the whole directory if it looks stale. The scripts rebuild it.
